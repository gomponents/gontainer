parameters:
  name: '%env("PERSON_NAM")%'

services:
  person:
    getter: "GetPerson"
    type: "Person"
    value: "Person{}"
    fields:
      Name: "%name%"
      Age: '%envInt("PERSON_AGE", 11)%'
